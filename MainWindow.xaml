<Window x:Class="WpfTouchFrameSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="1000" Width="1000">
    <Grid x:Name="grid1" Margin="0,0,258,216" TouchDown="OnTouchDown" TouchUp="OnTouchUp" IsManipulationEnabled="True" >
        <Canvas x:Name="canvas1"
                 Height="800 px" Width="800 px"
                Background="blue" Margin="106,38,-172,-84" VerticalAlignment="Top" 
                />
        <RichTextBox x:Name="Nummer" FontSize="40" 
                 Background="Tomato" Margin="248,162,286,392" Width="200" Height="200"/>
        <RichTextBox x:Name="xy" FontSize="20"
                 Background="Azure"  Width="200" Margin="534,162,0,0"/>
    </Grid>
</Window>